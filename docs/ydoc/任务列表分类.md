## 目标
src\renderer\components\Sidebar.tsx中的任务列表记录列表，会将所有的任务罗列出来，如果任务过多不易区分，我想增加一个任务归类的功能，用户可通过新建一个文件夹，将同类型的任务加到一个文件夹中，帮我评估该功能，然后实现它。

相关代码
```
      <div className="flex-1 overflow-y-auto px-2.5 pb-4">
        <div className="px-3 pb-2 text-sm font-medium dark:text-claude-darkTextSecondary text-claude-textSecondary">
          {i18nService.t('coworkHistory')}
        </div>
        <CoworkSessionList
          sessions={sessions}
          currentSessionId={currentSessionId}
          isBatchMode={isBatchMode}
          selectedIds={selectedIds}
          onSelectSession={handleSelectSession}
          onDeleteSession={handleDeleteSession}
          onTogglePin={handleTogglePin}
          onRenameSession={handleRenameSession}
          onToggleSelection={handleToggleSelection}
          onEnterBatchMode={handleEnterBatchMode}
        />
      </div>
```
## 准则
1、要遵循原有工具的界面风格。
2、需要调整后端数据库的结构，给每一个任务列表增加一个文件夹名称，默认可以为空
3、代码改动越少越好，尽量精简
4、不需要进行幻想型开发
