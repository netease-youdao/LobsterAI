diff --git a/src/logging/redact-patterns.ts b/src/logging/redact-patterns.ts
index 28d256b4521..e130622940f 100644
--- a/src/logging/redact-patterns.ts
+++ b/src/logging/redact-patterns.ts
@@ -79,7 +79,7 @@ const AMBIGUOUS_QUOTED_AUTH_FIELD_REDACT_PATTERN = String.raw`(^|[\s,{])["']?(?:
 // Pure-base64 prefixes require a non-alphanumeric boundary and skip explicit data-URL payloads.
 export const BASE64_SAFE_TOKEN_BOUNDARY = String.raw`(^|[^A-Za-z0-9])(?<!;base64,[A-Za-z0-9+/=]*)`;
 export const IDENTIFIER_SAFE_TOKEN_BOUNDARY = String.raw`(^|[^A-Za-z0-9_])`;
-const AWS_SECRET_ACCESS_KEY_VALUE_BOUNDARY = String.raw`(^|[^A-Za-z0-9/+=_])(?<!;base64,[A-Za-z0-9+/=]*)`;
+const AWS_SECRET_ACCESS_KEY_VALUE_BOUNDARY = String.raw`(^|[^A-Za-z0-9/+=_])(?<!;base64,[A-Za-z0-9+/=]*)(?<![A-Za-z]:)`;
 export const AWS_SECRET_ACCESS_KEY_VALUE_PATTERN = String.raw`(?=[A-Za-z0-9/+=]{40}(?![A-Za-z0-9/+=]))(?=[A-Za-z0-9/+=]{0,39}[A-Z])(?=[A-Za-z0-9/+=]{0,39}[a-z])(?=[A-Za-z0-9/+=]{0,39}[0-9/+=])(?=[A-Za-z0-9/+=]{0,39}[^A-Fa-f0-9])[A-Za-z0-9/+=]{40}`;
 const AWS_SECRET_ACCESS_KEY_VALUE_REDACT_PATTERN = String.raw`/${AWS_SECRET_ACCESS_KEY_VALUE_BOUNDARY}(${AWS_SECRET_ACCESS_KEY_VALUE_PATTERN})(?!_)/g`;
 const TELEGRAM_BOT_TOKEN_REDACT_PATTERN = String.raw`\bbot(\d{6,}:[A-Za-z0-9_-]{20,})\b`;
diff --git a/src/logging/redact.test.ts b/src/logging/redact.test.ts
index 83fceb7541a..a23f5a68ef7 100644
--- a/src/logging/redact.test.ts
+++ b/src/logging/redact.test.ts
@@ -288,4 +288,10 @@
     expect(output).toContain(`"SecretAccessKey":"${masked}"`);
     expect(output).toContain(`bare ${masked}`);
     expect(output).not.toContain(secret);
+
+    const windowsPath = "C:/Users/tester/lobsterai/project/chinajoy-ppt/deck.pptx";
+    const fileLink = `[deck](file:///${windowsPath})`;
+
+    expect(redactSensitiveText(windowsPath, { mode: "tools" })).toBe(windowsPath);
+    expect(redactSensitiveText(fileLink, { mode: "tools" })).toBe(fileLink);
   });
