diff --git a/packages/ai/src/providers/openai-completions-tool-calls.ts b/packages/ai/src/providers/openai-completions-tool-calls.ts
index 3669e1018a5..4a67d9da08b 100644
--- a/packages/ai/src/providers/openai-completions-tool-calls.ts
+++ b/packages/ai/src/providers/openai-completions-tool-calls.ts
@@ -2,13 +2,18 @@ import { randomUUID } from "node:crypto";
 import { isRecord } from "@openclaw/normalization-core/record-coerce";
 import type { ChatCompletionChunk } from "openai/resources/chat/completions.js";
 import { measureUtf8AppendBytes } from "../transports/openai-transport-shared.js";
-import { finalizeTerminalToolCallArguments } from "../transports/transport-stream-shared.js";
-import type { ToolCall } from "../types.js";
+import {
+  finalizeTerminalToolCallArguments,
+  readMalformedToolCallArgumentsDiagnostics,
+} from "../transports/transport-stream-shared.js";
+import type { AssistantMessageDiagnostic, ToolCall } from "../types.js";
 
 type ChatCompletionToolCallDelta = ChatCompletionChunk.Choice.Delta.ToolCall;
 const MAX_BUFFERED_TOOL_CALL_ARGUMENT_BYTES = 256_000;
 const MAX_BUFFERED_LEGACY_FOLLOWING_DELTA_BYTES = 256_000;
 const MAX_BUFFERED_LEGACY_FOLLOWING_DELTAS = 1_024;
+/** Terminal fact the embedded runner consumes to continue after a rejected, unexecuted call. */
+const MALFORMED_TOOL_CALL_DIAGNOSTIC_TYPE = "malformed_tool_call_arguments";
 
 type NormalizedOpenAICompletionsDelta = {
   delta: ChatCompletionChunk.Choice.Delta;
@@ -17,6 +22,8 @@ type NormalizedOpenAICompletionsDelta = {
 
 type OpenAICompletionsToolCallFinalizationOptions<TBlock extends object> = {
   allowSilentToolCallPromotion?: boolean;
+  /** Provider finish_reason mapped to a stop reason, recorded only in rejection diagnostics. */
+  providerStopReason?: string;
   onConfirmedToolCall?: (block: TBlock, contentIndex: number) => void;
 };
 
@@ -230,7 +237,12 @@ export function createOpenAICompletionsToolCallDeltaNormalizer(): (
 
 /** Publish only executable calls; streaming scratch state never belongs in replay. */
 export function finalizeOpenAICompletionsToolCalls<TBlock extends object>(
-  output: { content: TBlock[]; stopReason: string; errorMessage?: string },
+  output: {
+    content: TBlock[];
+    stopReason: string;
+    errorMessage?: string;
+    diagnostics?: AssistantMessageDiagnostic[];
+  },
   options: OpenAICompletionsToolCallFinalizationOptions<TBlock> = {},
 ): void {
   const isToolCall = (block: TBlock) => (block as { type?: unknown }).type === "toolCall";
@@ -269,27 +281,49 @@ export function finalizeOpenAICompletionsToolCalls<TBlock extends object>(
     partialArgs?: unknown;
   };
   const toolCalls = output.content.filter(isToolCall) as FinalToolCall[];
-  const rejectToolCalls = () => {
+  // A rejected call never executes. The diagnostic lets the runner continue the turn
+  // without matching error text; it records shape facts only, never argument text.
+  const rejectToolCalls = (details: Record<string, unknown>) => {
     output.stopReason = "error";
     output.errorMessage = "Provider returned an incomplete or malformed tool call";
     output.content = output.content.filter((candidate) => !isToolCall(candidate));
+    output.diagnostics = [
+      ...(output.diagnostics ?? []),
+      {
+        type: MALFORMED_TOOL_CALL_DIAGNOSTIC_TYPE,
+        timestamp: Date.now(),
+        details: {
+          toolCallCount: toolCalls.length,
+          ...(options.providerStopReason ? { providerStopReason: options.providerStopReason } : {}),
+          ...details,
+        },
+      },
+    ];
   };
-  if (
-    toolCalls.some(
-      (call) =>
-        typeof call.name !== "string" ||
-        call.name.trim().length === 0 ||
-        typeof call.partialArgs !== "string" ||
-        call.partialArgs.trim().length === 0,
-    )
-  ) {
-    rejectToolCalls();
+  const incompleteCall = toolCalls.find(
+    (call) =>
+      typeof call.name !== "string" ||
+      call.name.trim().length === 0 ||
+      typeof call.partialArgs !== "string" ||
+      call.partialArgs.trim().length === 0,
+  );
+  if (incompleteCall) {
+    const missingName =
+      typeof incompleteCall.name !== "string" || incompleteCall.name.trim().length === 0;
+    rejectToolCalls({ reason: missingName ? "missing_name" : "empty_arguments" });
     return;
   }
   try {
-    finalizeTerminalToolCallArguments(toolCalls, (call) => call.partialArgs);
-  } catch {
-    rejectToolCalls();
+    // OpenAI-compatible providers do not validate tool-call JSON server-side, so
+    // complete buffers get string-literal repair; truncated buffers stay rejected.
+    finalizeTerminalToolCallArguments(toolCalls, (call) => call.partialArgs, undefined, {
+      repairStringLiterals: true,
+    });
+  } catch (error) {
+    rejectToolCalls({
+      reason: "invalid_arguments",
+      ...readMalformedToolCallArgumentsDiagnostics(error),
+    });
     return;
   }
 
diff --git a/packages/ai/src/providers/openai-completions.legacy-function-call.test.ts b/packages/ai/src/providers/openai-completions.legacy-function-call.test.ts
index 85a2a94f458..ff1e2a49377 100644
--- a/packages/ai/src/providers/openai-completions.legacy-function-call.test.ts
+++ b/packages/ai/src/providers/openai-completions.legacy-function-call.test.ts
@@ -371,16 +371,45 @@ describe.each([
   );
 
   it.each([
-    { reason: "incomplete JSON", name: "lookup", arguments: '{"query":"cats"' },
-    { reason: "malformed JSON", name: "lookup", arguments: '{"query":}' },
-    { reason: "invalid string escape", name: "lookup", arguments: String.raw`{"query":"cats\q"}` },
-    { reason: "unescaped control character", name: "lookup", arguments: '{"query":"cats\n"}' },
-    { reason: "non-object JSON", name: "lookup", arguments: '["cats"]' },
-    { reason: "empty arguments", name: "lookup", arguments: "" },
-    { reason: "missing function name", name: "", arguments: '{"query":"cats"}' },
+    {
+      reason: "incomplete JSON",
+      name: "lookup",
+      arguments: '{"query":"cats"',
+      details: {
+        reason: "invalid_arguments",
+        argumentChars: 15,
+        repairAttempted: true,
+        parseError: "unexpected_token",
+        parseErrorPosition: 15,
+      },
+    },
+    {
+      reason: "malformed JSON",
+      name: "lookup",
+      arguments: '{"query":}',
+      details: { reason: "invalid_arguments", parseError: "unexpected_token" },
+    },
+    {
+      reason: "non-object JSON",
+      name: "lookup",
+      arguments: '["cats"]',
+      details: { reason: "invalid_arguments", parseError: "non_object" },
+    },
+    {
+      reason: "empty arguments",
+      name: "lookup",
+      arguments: "",
+      details: { reason: "empty_arguments" },
+    },
+    {
+      reason: "missing function name",
+      name: "",
+      arguments: '{"query":"cats"}',
+      details: { reason: "missing_name" },
+    },
   ] as const)(
     "rejects an authoritative modern tool terminal with $reason",
-    async ({ name, arguments: rawArguments }) => {
+    async ({ name, arguments: rawArguments, details }) => {
       const { eventTypes, result } = await collectFixture(
         confirmedModernCallChunks(rawArguments, { id: "call_malformed", name }),
       );
@@ -388,21 +417,82 @@ describe.each([
       expect(result.errorMessage).toContain("incomplete or malformed tool call");
       expect(result.content.filter((block) => block.type === "toolCall")).toHaveLength(0);
       expect(eventTypes).not.toContain("toolcall_end");
+      expect(result.diagnostics).toEqual([
+        {
+          type: "malformed_tool_call_arguments",
+          timestamp: expect.any(Number),
+          details: expect.objectContaining({ toolCallCount: 1, ...details }),
+        },
+      ]);
+      // Diagnostics describe the buffer's shape only and never retain argument text.
+      expect(JSON.stringify(result.diagnostics)).not.toContain("cats");
     },
   );
 
   it.each([
-    { reason: "incomplete JSON", arguments: '{"query":"cats"' },
-    { reason: "invalid string escape", arguments: String.raw`{"query":"cats\q"}` },
-    { reason: "unescaped control character", arguments: '{"query":"cats\n"}' },
-  ] as const)("rejects an authoritative legacy function terminal with $reason", async (value) => {
-    const { eventTypes, result } = await collectFixture(confirmedLegacyCallChunks(value.arguments));
+    {
+      reason: "invalid string escape",
+      arguments: String.raw`{"query":"cats\q"}`,
+      expected: { query: String.raw`cats\q` },
+    },
+    {
+      reason: "unescaped control character",
+      arguments: '{"query":"cats\n\tdogs"}',
+      expected: { query: "cats\n\tdogs" },
+    },
+  ] as const)(
+    "repairs a complete modern tool terminal with $reason",
+    async ({ arguments: rawArguments, expected }) => {
+      const { events, result } = await collectFixture(
+        confirmedModernCallChunks(rawArguments, { id: "call_repaired" }),
+      );
+      expect(result.stopReason).toBe("toolUse");
+      expect(result.content).toEqual([
+        expect.objectContaining({ type: "toolCall", id: "call_repaired", arguments: expected }),
+      ]);
+      expect(result.content[0]).not.toHaveProperty("partialArgs");
+      expect(result.diagnostics).toBeUndefined();
+      expect(events).toContainEqual({ type: "toolcall_end", toolCallId: "call_repaired" });
+    },
+  );
+
+  it("rejects an authoritative legacy function terminal with incomplete JSON", async () => {
+    const { eventTypes, result } = await collectFixture(
+      confirmedLegacyCallChunks('{"query":"cats"'),
+    );
     expect(result.stopReason).toBe("error");
     expect(result.errorMessage).toContain("incomplete or malformed tool call");
     expect(result.content.filter((block) => block.type === "toolCall")).toHaveLength(0);
     expect(eventTypes).not.toContain("toolcall_end");
+    expect(result.diagnostics?.[0]).toMatchObject({
+      type: "malformed_tool_call_arguments",
+      details: { reason: "invalid_arguments", providerStopReason: "toolUse" },
+    });
   });
 
+  it.each([
+    {
+      reason: "invalid string escape",
+      arguments: String.raw`{"query":"cats\q"}`,
+      expected: { query: String.raw`cats\q` },
+    },
+    {
+      reason: "unescaped control character",
+      arguments: '{"query":"cats\n"}',
+      expected: { query: "cats\n" },
+    },
+  ] as const)(
+    "repairs a complete legacy function terminal with $reason",
+    async ({ arguments: rawArguments, expected }) => {
+      const { eventTypes, result } = await collectFixture(confirmedLegacyCallChunks(rawArguments));
+      expect(result.stopReason).toBe("toolUse");
+      expect(result.content).toEqual([
+        expect.objectContaining({ type: "toolCall", name: "lookup", arguments: expected }),
+      ]);
+      expect(eventTypes).toContain("toolcall_end");
+    },
+  );
+
   it("rejects every parallel call when a sibling has incomplete arguments", async () => {
     const { eventTypes, result } = await collectFixture([
       chunk({
diff --git a/packages/ai/src/transports/openai-completions-stream.ts b/packages/ai/src/transports/openai-completions-stream.ts
index d45d4e6735f..bce39210a06 100644
--- a/packages/ai/src/transports/openai-completions-stream.ts
+++ b/packages/ai/src/transports/openai-completions-stream.ts
@@ -669,6 +669,7 @@ export async function processCompletionsStream(
   finalizeOpenAICompletionsToolCalls(output, {
     allowSilentToolCallPromotion:
       finishReason === "stop" || (sawNativeToolCallDelta && (options?.sawStreamDONE?.() ?? false)),
+    providerStopReason: finishReason,
     onConfirmedToolCall(block, contentIndex) {
       if (directMode || block.type !== "toolCall") {
         return;
diff --git a/packages/ai/src/transports/transport-stream-shared.test.ts b/packages/ai/src/transports/transport-stream-shared.test.ts
index 409d9435775..b65ebe1b5c1 100644
--- a/packages/ai/src/transports/transport-stream-shared.test.ts
+++ b/packages/ai/src/transports/transport-stream-shared.test.ts
@@ -1,8 +1,22 @@
 import { describe, expect, it } from "vitest";
-import { parseTerminalToolCallArguments } from "./transport-stream-shared.js";
+import {
+  finalizeTerminalToolCallArguments,
+  parseTerminalToolCallArguments,
+  readMalformedToolCallArgumentsDiagnostics,
+} from "./transport-stream-shared.js";
 
 const MALFORMED_TOOL_CALL_TERMINAL_ERROR_MESSAGE =
   "Provider completed tool call with malformed JSON arguments";
+const REPAIR = { repairStringLiterals: true } as const;
+
+function captureError(run: () => unknown): Error & { errorCode?: string; errorBody?: string } {
+  try {
+    run();
+  } catch (error) {
+    return error as Error & { errorCode?: string; errorBody?: string };
+  }
+  throw new Error("expected a throw");
+}
 
 describe("parseTerminalToolCallArguments", () => {
   it("preserves unsafe integer literals in complete object arguments", () => {
@@ -13,17 +27,151 @@ describe("parseTerminalToolCallArguments", () => {
     expect(parseTerminalToolCallArguments({})).toEqual({});
   });
 
-  it.each(["", "   ", '{"secret":"do-not-echo"', "[]", "null", null])(
-    "rejects non-object or malformed terminal input %# without exposing it",
-    (value) => {
-      let thrown: unknown;
-      try {
-        parseTerminalToolCallArguments(value);
-      } catch (error) {
-        thrown = error;
-      }
+  it("stays strict by default: raw control characters are rejected without repair", () => {
+    const thrown = captureError(() =>
+      parseTerminalToolCallArguments('{"path":"a.py","newText":"x = 1\ny = 2"}'),
+    );
+    expect(thrown.message).toBe(MALFORMED_TOOL_CALL_TERMINAL_ERROR_MESSAGE);
+    expect(readMalformedToolCallArgumentsDiagnostics(thrown)).toMatchObject({
+      repairAttempted: false,
+    });
+  });
+
+  it("repairs raw control characters inside complete string values when opted in", () => {
+    expect(
+      parseTerminalToolCallArguments(
+        '{"path":"a.py","newText":"x = 1\ny = 2\tend"}',
+        undefined,
+        REPAIR,
+      ),
+    ).toEqual({ path: "a.py", newText: "x = 1\ny = 2\tend" });
+  });
+
+  it("repairs invalid escapes by preserving the backslash the model meant", () => {
+    // JS string is: {"command":"grep -E \d+ file"} — an unescaped regex backslash.
+    expect(
+      parseTerminalToolCallArguments('{"command":"grep -E \\d+ file"}', undefined, REPAIR),
+    ).toEqual({ command: "grep -E \\d+ file" });
+  });
+
+  it("preserves valid control escapes in sibling fields, even after a path-like prefix", () => {
+    // oldText legitimately contains a newline (a valid \n escape after "C:"); newText has a raw
+    // newline that needs repair. The repair must not turn oldText into a literal backslash-n.
+    const raw = '{"path":"C:\\\\app\\\\a.py","oldText":"C:\\nnext","newText":"x = 1\ny = 2"}';
+    expect(parseTerminalToolCallArguments(raw, undefined, REPAIR)).toEqual({
+      path: "C:\\app\\a.py",
+      oldText: "C:\nnext",
+      newText: "x = 1\ny = 2",
+    });
+  });
+
+  it("keeps unsafe integers intact when a repair was needed", () => {
+    expect(
+      parseTerminalToolCallArguments('{"id":9223372036854775807,"note":"a\nb"}', undefined, REPAIR),
+    ).toEqual({ id: "9223372036854775807", note: "a\nb" });
+  });
+
+  it.each([
+    "",
+    "   ",
+    '{"secret":"do-not-echo"',
+    "[]",
+    "null",
+    null,
+    // Truncated free-text arguments must never be "repaired" into a shorter, executable
+    // command (a cut-off `rm -rf /srv/app/tmp/build-cache` would otherwise become `rm -rf /`).
+    '{"command":"rm -rf /',
+    '{"command":"rm -rf /srv/app/tmp/bu',
+    '{"command":"rm -rf /srv/app/tmp/build-cache","timeout":6',
+    // An unescaped quote ends the string early; string-literal repair cannot guess intent.
+    '{"note":"he said "do-not-echo" twice"}',
+  ])("rejects non-object or malformed terminal input %# without exposing it", (value) => {
+    for (const options of [undefined, REPAIR]) {
+      const thrown = captureError(() => parseTerminalToolCallArguments(value, undefined, options));
       expect(thrown).toMatchObject({ message: MALFORMED_TOOL_CALL_TERMINAL_ERROR_MESSAGE });
-      expect(String(thrown)).not.toContain("do-not-echo");
-    },
-  );
+      const surfaced = `${String(thrown)}${JSON.stringify(readMalformedToolCallArgumentsDiagnostics(thrown) ?? null)}`;
+      expect(surfaced).not.toContain("do-not-echo");
+      expect(surfaced).not.toContain("rm -rf");
+    }
+  });
+
+  it("attaches privacy-safe diagnostics without changing projected error fields", () => {
+    const truncated = '{"secret":"do-not-echo"';
+    const thrown = captureError(() => parseTerminalToolCallArguments(truncated, undefined, REPAIR));
+    expect(readMalformedToolCallArgumentsDiagnostics(thrown)).toEqual({
+      argumentChars: truncated.length,
+      argumentHash: expect.stringMatching(/^[0-9a-z]+$/),
+      repairAttempted: true,
+      parseError: "unexpected_token",
+      parseErrorPosition: truncated.length,
+    });
+    // Transports that surface this error keep their existing projection and failover inputs.
+    expect(thrown.cause).toBeUndefined();
+    expect(thrown).not.toHaveProperty("code");
+    expect(thrown.errorCode).toBeUndefined();
+    expect(thrown.errorBody).toBeUndefined();
+  });
+
+  it.each([
+    { value: "", parseError: "empty" },
+    { value: "[1]", parseError: "non_object" },
+    { value: null, parseError: "non_object" },
+    { value: '{"a":"unterminated', parseError: "unterminated_string", parseErrorPosition: 18 },
+    { value: '{"a":"x\ny"}', parseError: "bad_control_character", parseErrorPosition: 7 },
+    { value: '{"a":"x\\qy"}', parseError: "bad_escape", parseErrorPosition: 8 },
+    { value: '{"a":1} trailing', parseError: "trailing_content", parseErrorPosition: 8 },
+    { value: '{"a":}', parseError: "unexpected_token" },
+  ] as const)("classifies the unrepaired JSON failure of $value", (fixture) => {
+    const thrown = captureError(() => parseTerminalToolCallArguments(fixture.value));
+    const diagnostics = readMalformedToolCallArgumentsDiagnostics(thrown);
+    expect(diagnostics?.parseError).toBe(fixture.parseError);
+    expect(diagnostics?.parseErrorPosition).toBe(
+      "parseErrorPosition" in fixture ? fixture.parseErrorPosition : undefined,
+    );
+  });
+
+  it("ignores errors that do not carry malformed-argument diagnostics", () => {
+    expect(readMalformedToolCallArgumentsDiagnostics(new Error("other"))).toBeUndefined();
+    expect(readMalformedToolCallArgumentsDiagnostics("text")).toBeUndefined();
+  });
+});
+
+describe("finalizeTerminalToolCallArguments", () => {
+  it("repairs a sibling call without touching already-valid siblings", () => {
+    const calls = [
+      {
+        name: "read",
+        arguments: {} as Record<string, unknown>,
+        partialJson: '{"path":"README.md"}',
+      },
+      {
+        name: "edit",
+        arguments: {} as Record<string, unknown>,
+        partialJson: '{"path":"a.py","newText":"x\ny"}',
+      },
+    ];
+    finalizeTerminalToolCallArguments(calls, (call) => call.partialJson, undefined, REPAIR);
+    expect(calls[0]?.arguments).toEqual({ path: "README.md" });
+    expect(calls[1]?.arguments).toEqual({ path: "a.py", newText: "x\ny" });
+  });
+
+  it("does not mutate any sibling when one call stays malformed", () => {
+    const calls = [
+      {
+        name: "read",
+        arguments: {} as Record<string, unknown>,
+        partialJson: '{"path":"README.md"}',
+      },
+      {
+        name: "edit",
+        arguments: {} as Record<string, unknown>,
+        partialJson: '{"path":"a.py","newText":"x',
+      },
+    ];
+    expect(() =>
+      finalizeTerminalToolCallArguments(calls, (call) => call.partialJson, undefined, REPAIR),
+    ).toThrow(MALFORMED_TOOL_CALL_TERMINAL_ERROR_MESSAGE);
+    expect(calls[0]?.arguments).toEqual({});
+    expect(calls[1]?.arguments).toEqual({});
+  });
 });
diff --git a/packages/ai/src/transports/transport-stream-shared.ts b/packages/ai/src/transports/transport-stream-shared.ts
index 5f68293ee19..109eb0f16b0 100644
--- a/packages/ai/src/transports/transport-stream-shared.ts
+++ b/packages/ai/src/transports/transport-stream-shared.ts
@@ -12,10 +12,15 @@ import type {
 } from "@openclaw/llm-core";
 import { asNonArrayRecord, asOptionalRecord } from "@openclaw/normalization-core/record-coerce";
 import { createAssistantMessageEventStream } from "../utils/event-stream.js";
+import { shortHash } from "../utils/hash.js";
 import { headersToRecord } from "../utils/headers.js";
+import { repairJson } from "../utils/json-parse.js";
 import { projectProviderError, type ProviderErrorProjection } from "../utils/provider-error.js";
 import { sanitizeSurrogates } from "../utils/sanitize-unicode.js";
-import { parseJsonObjectPreservingUnsafeIntegers } from "./json-unsafe-integers.js";
+import {
+  parseJsonObjectPreservingUnsafeIntegers,
+  quoteUnsafeIntegerLiterals,
+} from "./json-unsafe-integers.js";
 
 type ContextUsage = NonNullable<Usage["contextUsage"]>;
 
@@ -68,16 +73,139 @@ export function coerceTransportToolCallArguments(argumentsValue: unknown): Recor
   return {};
 }
 
-/** Admit only complete object-shaped terminal tool arguments; partial parsing is preview-only. */
+/** Content-free class of the JSON failure a provider's terminal argument buffer produced. */
+type MalformedToolCallArgumentsParseError =
+  | "empty"
+  | "non_object"
+  | "unexpected_end"
+  | "unterminated_string"
+  | "bad_control_character"
+  | "bad_escape"
+  | "trailing_content"
+  | "unexpected_token";
+
+/**
+ * Bounded, content-free diagnostics for a rejected terminal argument buffer. The parse
+ * error class and position separate truncated buffers (failure at the end) from complete
+ * buffers the model wrote invalid JSON into, without retaining any argument text.
+ */
+export type MalformedToolCallArgumentsDiagnostics = {
+  argumentChars: number;
+  argumentHash: string;
+  repairAttempted: boolean;
+  parseError: MalformedToolCallArgumentsParseError;
+  parseErrorPosition?: number;
+};
+
+/**
+ * Rejected terminal tool-call arguments. Diagnostics stay off `code`, `cause`, and
+ * `errorBody` so provider error projection (and failover classification) of transports
+ * that surface this error is unchanged.
+ */
+class MalformedToolCallArgumentsError extends Error {
+  readonly diagnostics: MalformedToolCallArgumentsDiagnostics;
+
+  constructor(message: string, diagnostics: MalformedToolCallArgumentsDiagnostics) {
+    super(message);
+    this.diagnostics = diagnostics;
+  }
+}
+
+const JSON_PARSE_ERROR_CLASSES: ReadonlyArray<
+  readonly [RegExp, MalformedToolCallArgumentsParseError]
+> = [
+  [/Unexpected end of JSON input/i, "unexpected_end"],
+  [/Unterminated string/i, "unterminated_string"],
+  [/Bad control character/i, "bad_control_character"],
+  [/Bad (?:escaped character|Unicode escape)/i, "bad_escape"],
+  [/Unexpected non-whitespace character after JSON/i, "trailing_content"],
+];
+
+function classifyMalformedToolCallArguments(value: unknown): {
+  parseError: MalformedToolCallArgumentsParseError;
+  parseErrorPosition?: number;
+} {
+  if (typeof value !== "string") {
+    return { parseError: "non_object" };
+  }
+  if (value.trim().length === 0) {
+    return { parseError: "empty" };
+  }
+  try {
+    JSON.parse(quoteUnsafeIntegerLiterals(value));
+    return { parseError: "non_object" };
+  } catch (error) {
+    // Only the engine's fixed wording is inspected; the message itself can quote
+    // argument text, so it is never stored.
+    const message = error instanceof Error ? error.message : "";
+    const parseError =
+      JSON_PARSE_ERROR_CLASSES.find(([pattern]) => pattern.test(message))?.[1] ??
+      "unexpected_token";
+    const position = /\bat position (\d+)/.exec(message)?.[1];
+    return position === undefined
+      ? { parseError }
+      : { parseError, parseErrorPosition: Number(position) };
+  }
+}
+
+function createMalformedToolCallArgumentsError(
+  value: unknown,
+  errorMessage: string,
+  repairAttempted: boolean,
+): MalformedToolCallArgumentsError {
+  const text = typeof value === "string" ? value : undefined;
+  return new MalformedToolCallArgumentsError(errorMessage, {
+    argumentChars: text?.length ?? 0,
+    argumentHash: text === undefined ? "" : shortHash(text),
+    repairAttempted,
+    ...classifyMalformedToolCallArguments(value),
+  });
+}
+
+/** Reads the content-free diagnostics attached by a rejected terminal argument parse. */
+export function readMalformedToolCallArgumentsDiagnostics(
+  error: unknown,
+): MalformedToolCallArgumentsDiagnostics | undefined {
+  return error instanceof MalformedToolCallArgumentsError ? error.diagnostics : undefined;
+}
+
+/**
+ * Repair a complete-but-invalid terminal argument buffer. Providers that skip server-side
+ * JSON validation can finish a tool call whose string values carry raw control characters
+ * or invalid escapes. Only string-literal repairs are applied and every valid escape is
+ * preserved as written; truncated or non-object buffers stay rejected so a cut-off write
+ * never executes with partial arguments.
+ */
+function repairTerminalToolCallArguments(value: string): Record<string, unknown> | null {
+  const repaired = repairJson(value, { preserveValidControlEscapes: true });
+  if (repaired === value) {
+    return null;
+  }
+  return parseJsonObjectPreservingUnsafeIntegers(repaired);
+}
+
+/**
+ * Admit only complete object-shaped terminal tool arguments; partial parsing is preview-only.
+ * `repairStringLiterals` opts a stream whose provider may deliver unvalidated tool input into
+ * string-literal repair before rejection.
+ */
 export function parseTerminalToolCallArguments(
   value: unknown,
   errorMessage = MALFORMED_TOOL_CALL_TERMINAL_ERROR_MESSAGE,
+  options?: { repairStringLiterals?: boolean },
 ): Record<string, unknown> {
   const parsed = parseJsonObjectPreservingUnsafeIntegers(value);
-  if (!parsed) {
-    throw new Error(errorMessage);
+  if (parsed) {
+    return parsed;
+  }
+  const repairStringLiterals = options?.repairStringLiterals === true && typeof value === "string";
+  if (repairStringLiterals) {
+    const repaired = repairTerminalToolCallArguments(value);
+    if (repaired) {
+      return repaired;
+    }
   }
-  return parsed;
+  throw createMalformedToolCallArgumentsError(value, errorMessage, repairStringLiterals);
 }
 
 /** Validate a complete sibling set before mutating any call into executable state. */
@@ -85,9 +213,11 @@ export function finalizeTerminalToolCallArguments<T extends { arguments: Record<
   calls: readonly T[],
   readArguments: (call: T) => unknown,
   errorMessage?: string,
+  options?: { repairStringLiterals?: boolean },
 ): void {
   const validated = calls.map(
-    (call) => [call, parseTerminalToolCallArguments(readArguments(call), errorMessage)] as const,
+    (call) =>
+      [call, parseTerminalToolCallArguments(readArguments(call), errorMessage, options)] as const,
   );
   for (const [call, argumentsValue] of validated) {
     call.arguments = argumentsValue;
diff --git a/packages/ai/src/utils/json-parse.ts b/packages/ai/src/utils/json-parse.ts
index d72373c9602..23f507b087f 100644
--- a/packages/ai/src/utils/json-parse.ts
+++ b/packages/ai/src/utils/json-parse.ts
@@ -31,8 +31,21 @@ function escapeControlCharacter(char: string): string {
  * Repairs malformed JSON string literals by:
  * - escaping raw control characters inside strings
  * - doubling backslashes before invalid escape characters
+ *
+ * By default a valid control escape (`\n`, `\t`, ...) that follows a Windows-path-looking
+ * prefix is treated as an unescaped path separator and doubled. Pass
+ * `preserveValidControlEscapes` when the text is authoritative (for example a completed
+ * tool-call argument buffer) and every valid escape must survive as written.
  */
-export function repairJson(json: string): string {
+export function repairJson(
+  json: string,
+  options?: { preserveValidControlEscapes?: boolean },
+): string {
+  // oxlint-disable-next-line no-control-regex -- JSON string repair must detect raw control characters.
+  if (!/[\\\x00-\x1f]/.test(json)) {
+    return json;
+  }
+  const preserveValidControlEscapes = options?.preserveValidControlEscapes === true;
   let repaired = "";
   let inString = false;
   let stringValuePrefix = "";
@@ -80,7 +93,11 @@ export function repairJson(json: string): string {
         continue;
       }
 
-      if (JSON_CONTROL_ESCAPES.has(nextChar) && looksLikeWindowsPathPrefix(stringValuePrefix)) {
+      if (
+        !preserveValidControlEscapes &&
+        JSON_CONTROL_ESCAPES.has(nextChar) &&
+        looksLikeWindowsPathPrefix(stringValuePrefix)
+      ) {
         repaired += "\\\\";
         stringValuePrefix += "\\";
         continue;
